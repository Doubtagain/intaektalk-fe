# openapi-typescript(devDependency, API 코드 생성 전용)의 peer 가 typescript@^5 를
# 요구하지만 프로젝트는 typescript@6 을 쓴다. 빌드(tsc -b && vite build)와는 무관하므로
# peer 충돌을 무시한다. (Cloudflare Pages 의 npm ci 가 이 설정으로 통과한다.)
legacy-peer-deps=true
